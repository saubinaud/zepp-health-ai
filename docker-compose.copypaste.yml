# ============================================
# ZEPP HEALTH AI - COPIAR Y PEGAR EN EASYPANEL
# ============================================
#
# INSTRUCCIONES:
# 1. Copia TODO este archivo
# 2. Pégalo en EasyPanel → Create Service → Docker Compose
# 3. Configura SOLO 3 variables de entorno:
#    POSTGRES_PASSWORD=tu_password_aqui
#    JWT_SECRET=un_secret_aleatorio_largo
#    OPENAI_API_KEY=sk-tu-api-key-openai
# 4. ¡Deploy!
#
# IMPORTANTE: EasyPanel necesitará acceso al código para build.
# Si no quieres usar Git, ve a la Opción 2 en README.md
#
# ============================================

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-zepp_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-zepp_health}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-zepp_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: node:20-alpine
    restart: unless-stopped
    working_dir: /app
    command: sh -c "npm install && npm run build && npm start"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-zepp_user}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-zepp_health}
      NODE_ENV: production
      PORT: 3001
      JWT_SECRET: ${JWT_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4-turbo-preview}
      OPENAI_MAX_TOKENS: ${OPENAI_MAX_TOKENS:-2000}
      SYNC_INTERVAL_MINUTES: ${SYNC_INTERVAL_MINUTES:-60}
      RATE_LIMIT_REQUESTS_PER_SECOND: ${RATE_LIMIT_REQUESTS_PER_SECOND:-1}
      ENABLE_NOTIFICATIONS: ${ENABLE_NOTIFICATIONS:-true}
      NOTIFICATION_HR_HIGH: ${NOTIFICATION_HR_HIGH:-120}
      NOTIFICATION_HR_LOW: ${NOTIFICATION_HR_LOW:-45}
      NOTIFICATION_SPO2_LOW: ${NOTIFICATION_SPO2_LOW:-92}
      NOTIFICATION_STRESS_HIGH: ${NOTIFICATION_STRESS_HIGH:-80}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
    volumes:
      - ./backend:/app
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: node:20-alpine
    restart: unless-stopped
    working_dir: /app
    command: sh -c "npm install && npm run build && npm start"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-ws://localhost:3001}
      NODE_ENV: production
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
